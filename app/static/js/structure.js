document.addEventListener('DOMContentLoaded', async () => {const el=document.getElementById('viewer'); if(!el || !window.$3Dmol) return; const url=el.dataset.cif; try{const res=await fetch(url); if(!res.ok) throw new Error('CIF unavailable'); const cif=await res.text(); const viewer=$3Dmol.createViewer(el,{backgroundColor:'white'}); viewer.addModel(cif,'cif'); viewer.setStyle({}, {cartoon:{color:'spectrum'}}); viewer.zoomTo(); viewer.render(); viewer.spin(false);}catch(e){el.innerHTML='<div class="empty-state">Structure could not be loaded.</div>';}});
